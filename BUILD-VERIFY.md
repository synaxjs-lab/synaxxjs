# SYNAX Build Verification

Use the same build locally that Vercel uses:

```bash
npm install
npm run build
npm start
```

The project root is the only canonical application root. The previous nested `SYNAX/` duplicate has been removed.
